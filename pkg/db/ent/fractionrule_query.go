// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/fractionrule"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/predicate"
)

// FractionRuleQuery is the builder for querying FractionRule entities.
type FractionRuleQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FractionRule
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FractionRuleQuery builder.
func (frq *FractionRuleQuery) Where(ps ...predicate.FractionRule) *FractionRuleQuery {
	frq.predicates = append(frq.predicates, ps...)
	return frq
}

// Limit adds a limit step to the query.
func (frq *FractionRuleQuery) Limit(limit int) *FractionRuleQuery {
	frq.limit = &limit
	return frq
}

// Offset adds an offset step to the query.
func (frq *FractionRuleQuery) Offset(offset int) *FractionRuleQuery {
	frq.offset = &offset
	return frq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (frq *FractionRuleQuery) Unique(unique bool) *FractionRuleQuery {
	frq.unique = &unique
	return frq
}

// Order adds an order step to the query.
func (frq *FractionRuleQuery) Order(o ...OrderFunc) *FractionRuleQuery {
	frq.order = append(frq.order, o...)
	return frq
}

// First returns the first FractionRule entity from the query.
// Returns a *NotFoundError when no FractionRule was found.
func (frq *FractionRuleQuery) First(ctx context.Context) (*FractionRule, error) {
	nodes, err := frq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fractionrule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (frq *FractionRuleQuery) FirstX(ctx context.Context) *FractionRule {
	node, err := frq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FractionRule ID from the query.
// Returns a *NotFoundError when no FractionRule ID was found.
func (frq *FractionRuleQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = frq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fractionrule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (frq *FractionRuleQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := frq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FractionRule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FractionRule entity is found.
// Returns a *NotFoundError when no FractionRule entities are found.
func (frq *FractionRuleQuery) Only(ctx context.Context) (*FractionRule, error) {
	nodes, err := frq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fractionrule.Label}
	default:
		return nil, &NotSingularError{fractionrule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (frq *FractionRuleQuery) OnlyX(ctx context.Context) *FractionRule {
	node, err := frq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FractionRule ID in the query.
// Returns a *NotSingularError when more than one FractionRule ID is found.
// Returns a *NotFoundError when no entities are found.
func (frq *FractionRuleQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = frq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fractionrule.Label}
	default:
		err = &NotSingularError{fractionrule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (frq *FractionRuleQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := frq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FractionRules.
func (frq *FractionRuleQuery) All(ctx context.Context) ([]*FractionRule, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return frq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (frq *FractionRuleQuery) AllX(ctx context.Context) []*FractionRule {
	nodes, err := frq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FractionRule IDs.
func (frq *FractionRuleQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := frq.Select(fractionrule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (frq *FractionRuleQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := frq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (frq *FractionRuleQuery) Count(ctx context.Context) (int, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return frq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (frq *FractionRuleQuery) CountX(ctx context.Context) int {
	count, err := frq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (frq *FractionRuleQuery) Exist(ctx context.Context) (bool, error) {
	if err := frq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return frq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (frq *FractionRuleQuery) ExistX(ctx context.Context) bool {
	exist, err := frq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FractionRuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (frq *FractionRuleQuery) Clone() *FractionRuleQuery {
	if frq == nil {
		return nil
	}
	return &FractionRuleQuery{
		config:     frq.config,
		limit:      frq.limit,
		offset:     frq.offset,
		order:      append([]OrderFunc{}, frq.order...),
		predicates: append([]predicate.FractionRule{}, frq.predicates...),
		// clone intermediate query.
		sql:    frq.sql.Clone(),
		path:   frq.path,
		unique: frq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FractionRule.Query().
//		GroupBy(fractionrule.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (frq *FractionRuleQuery) GroupBy(field string, fields ...string) *FractionRuleGroupBy {
	grbuild := &FractionRuleGroupBy{config: frq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := frq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return frq.sqlQuery(ctx), nil
	}
	grbuild.label = fractionrule.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.FractionRule.Query().
//		Select(fractionrule.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (frq *FractionRuleQuery) Select(fields ...string) *FractionRuleSelect {
	frq.fields = append(frq.fields, fields...)
	selbuild := &FractionRuleSelect{FractionRuleQuery: frq}
	selbuild.label = fractionrule.Label
	selbuild.flds, selbuild.scan = &frq.fields, selbuild.Scan
	return selbuild
}

func (frq *FractionRuleQuery) prepareQuery(ctx context.Context) error {
	for _, f := range frq.fields {
		if !fractionrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if frq.path != nil {
		prev, err := frq.path(ctx)
		if err != nil {
			return err
		}
		frq.sql = prev
	}
	if fractionrule.Policy == nil {
		return errors.New("ent: uninitialized fractionrule.Policy (forgotten import ent/runtime?)")
	}
	if err := fractionrule.Policy.EvalQuery(ctx, frq); err != nil {
		return err
	}
	return nil
}

func (frq *FractionRuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FractionRule, error) {
	var (
		nodes = []*FractionRule{}
		_spec = frq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FractionRule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FractionRule{config: frq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(frq.modifiers) > 0 {
		_spec.Modifiers = frq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, frq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (frq *FractionRuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := frq.querySpec()
	if len(frq.modifiers) > 0 {
		_spec.Modifiers = frq.modifiers
	}
	_spec.Node.Columns = frq.fields
	if len(frq.fields) > 0 {
		_spec.Unique = frq.unique != nil && *frq.unique
	}
	return sqlgraph.CountNodes(ctx, frq.driver, _spec)
}

func (frq *FractionRuleQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := frq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (frq *FractionRuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   fractionrule.Table,
			Columns: fractionrule.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: fractionrule.FieldID,
			},
		},
		From:   frq.sql,
		Unique: true,
	}
	if unique := frq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := frq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fractionrule.FieldID)
		for i := range fields {
			if fields[i] != fractionrule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := frq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := frq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := frq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := frq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (frq *FractionRuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(frq.driver.Dialect())
	t1 := builder.Table(fractionrule.Table)
	columns := frq.fields
	if len(columns) == 0 {
		columns = fractionrule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if frq.sql != nil {
		selector = frq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if frq.unique != nil && *frq.unique {
		selector.Distinct()
	}
	for _, m := range frq.modifiers {
		m(selector)
	}
	for _, p := range frq.predicates {
		p(selector)
	}
	for _, p := range frq.order {
		p(selector)
	}
	if offset := frq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := frq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (frq *FractionRuleQuery) ForUpdate(opts ...sql.LockOption) *FractionRuleQuery {
	if frq.driver.Dialect() == dialect.Postgres {
		frq.Unique(false)
	}
	frq.modifiers = append(frq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return frq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (frq *FractionRuleQuery) ForShare(opts ...sql.LockOption) *FractionRuleQuery {
	if frq.driver.Dialect() == dialect.Postgres {
		frq.Unique(false)
	}
	frq.modifiers = append(frq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return frq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (frq *FractionRuleQuery) Modify(modifiers ...func(s *sql.Selector)) *FractionRuleSelect {
	frq.modifiers = append(frq.modifiers, modifiers...)
	return frq.Select()
}

// FractionRuleGroupBy is the group-by builder for FractionRule entities.
type FractionRuleGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (frgb *FractionRuleGroupBy) Aggregate(fns ...AggregateFunc) *FractionRuleGroupBy {
	frgb.fns = append(frgb.fns, fns...)
	return frgb
}

// Scan applies the group-by query and scans the result into the given value.
func (frgb *FractionRuleGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := frgb.path(ctx)
	if err != nil {
		return err
	}
	frgb.sql = query
	return frgb.sqlScan(ctx, v)
}

func (frgb *FractionRuleGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range frgb.fields {
		if !fractionrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := frgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := frgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (frgb *FractionRuleGroupBy) sqlQuery() *sql.Selector {
	selector := frgb.sql.Select()
	aggregation := make([]string, 0, len(frgb.fns))
	for _, fn := range frgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(frgb.fields)+len(frgb.fns))
		for _, f := range frgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(frgb.fields...)...)
}

// FractionRuleSelect is the builder for selecting fields of FractionRule entities.
type FractionRuleSelect struct {
	*FractionRuleQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (frs *FractionRuleSelect) Scan(ctx context.Context, v interface{}) error {
	if err := frs.prepareQuery(ctx); err != nil {
		return err
	}
	frs.sql = frs.FractionRuleQuery.sqlQuery(ctx)
	return frs.sqlScan(ctx, v)
}

func (frs *FractionRuleSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := frs.sql.Query()
	if err := frs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (frs *FractionRuleSelect) Modify(modifiers ...func(s *sql.Selector)) *FractionRuleSelect {
	frs.modifiers = append(frs.modifiers, modifiers...)
	return frs
}
