// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/gooduser"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/predicate"
)

// GoodUserQuery is the builder for querying GoodUser entities.
type GoodUserQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodUser
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodUserQuery builder.
func (guq *GoodUserQuery) Where(ps ...predicate.GoodUser) *GoodUserQuery {
	guq.predicates = append(guq.predicates, ps...)
	return guq
}

// Limit adds a limit step to the query.
func (guq *GoodUserQuery) Limit(limit int) *GoodUserQuery {
	guq.limit = &limit
	return guq
}

// Offset adds an offset step to the query.
func (guq *GoodUserQuery) Offset(offset int) *GoodUserQuery {
	guq.offset = &offset
	return guq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (guq *GoodUserQuery) Unique(unique bool) *GoodUserQuery {
	guq.unique = &unique
	return guq
}

// Order adds an order step to the query.
func (guq *GoodUserQuery) Order(o ...OrderFunc) *GoodUserQuery {
	guq.order = append(guq.order, o...)
	return guq
}

// First returns the first GoodUser entity from the query.
// Returns a *NotFoundError when no GoodUser was found.
func (guq *GoodUserQuery) First(ctx context.Context) (*GoodUser, error) {
	nodes, err := guq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{gooduser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (guq *GoodUserQuery) FirstX(ctx context.Context) *GoodUser {
	node, err := guq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodUser ID from the query.
// Returns a *NotFoundError when no GoodUser ID was found.
func (guq *GoodUserQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = guq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{gooduser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (guq *GoodUserQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := guq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodUser entity is found.
// Returns a *NotFoundError when no GoodUser entities are found.
func (guq *GoodUserQuery) Only(ctx context.Context) (*GoodUser, error) {
	nodes, err := guq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{gooduser.Label}
	default:
		return nil, &NotSingularError{gooduser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (guq *GoodUserQuery) OnlyX(ctx context.Context) *GoodUser {
	node, err := guq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodUser ID in the query.
// Returns a *NotSingularError when more than one GoodUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (guq *GoodUserQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = guq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{gooduser.Label}
	default:
		err = &NotSingularError{gooduser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (guq *GoodUserQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := guq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodUsers.
func (guq *GoodUserQuery) All(ctx context.Context) ([]*GoodUser, error) {
	if err := guq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return guq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (guq *GoodUserQuery) AllX(ctx context.Context) []*GoodUser {
	nodes, err := guq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodUser IDs.
func (guq *GoodUserQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := guq.Select(gooduser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (guq *GoodUserQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := guq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (guq *GoodUserQuery) Count(ctx context.Context) (int, error) {
	if err := guq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return guq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (guq *GoodUserQuery) CountX(ctx context.Context) int {
	count, err := guq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (guq *GoodUserQuery) Exist(ctx context.Context) (bool, error) {
	if err := guq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return guq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (guq *GoodUserQuery) ExistX(ctx context.Context) bool {
	exist, err := guq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (guq *GoodUserQuery) Clone() *GoodUserQuery {
	if guq == nil {
		return nil
	}
	return &GoodUserQuery{
		config:     guq.config,
		limit:      guq.limit,
		offset:     guq.offset,
		order:      append([]OrderFunc{}, guq.order...),
		predicates: append([]predicate.GoodUser{}, guq.predicates...),
		// clone intermediate query.
		sql:    guq.sql.Clone(),
		path:   guq.path,
		unique: guq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodUser.Query().
//		GroupBy(gooduser.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (guq *GoodUserQuery) GroupBy(field string, fields ...string) *GoodUserGroupBy {
	grbuild := &GoodUserGroupBy{config: guq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := guq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return guq.sqlQuery(ctx), nil
	}
	grbuild.label = gooduser.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.GoodUser.Query().
//		Select(gooduser.FieldCreatedAt).
//		Scan(ctx, &v)
func (guq *GoodUserQuery) Select(fields ...string) *GoodUserSelect {
	guq.fields = append(guq.fields, fields...)
	selbuild := &GoodUserSelect{GoodUserQuery: guq}
	selbuild.label = gooduser.Label
	selbuild.flds, selbuild.scan = &guq.fields, selbuild.Scan
	return selbuild
}

func (guq *GoodUserQuery) prepareQuery(ctx context.Context) error {
	for _, f := range guq.fields {
		if !gooduser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if guq.path != nil {
		prev, err := guq.path(ctx)
		if err != nil {
			return err
		}
		guq.sql = prev
	}
	if gooduser.Policy == nil {
		return errors.New("ent: uninitialized gooduser.Policy (forgotten import ent/runtime?)")
	}
	if err := gooduser.Policy.EvalQuery(ctx, guq); err != nil {
		return err
	}
	return nil
}

func (guq *GoodUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodUser, error) {
	var (
		nodes = []*GoodUser{}
		_spec = guq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*GoodUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &GoodUser{config: guq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(guq.modifiers) > 0 {
		_spec.Modifiers = guq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, guq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (guq *GoodUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := guq.querySpec()
	if len(guq.modifiers) > 0 {
		_spec.Modifiers = guq.modifiers
	}
	_spec.Node.Columns = guq.fields
	if len(guq.fields) > 0 {
		_spec.Unique = guq.unique != nil && *guq.unique
	}
	return sqlgraph.CountNodes(ctx, guq.driver, _spec)
}

func (guq *GoodUserQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := guq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (guq *GoodUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   gooduser.Table,
			Columns: gooduser.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: gooduser.FieldID,
			},
		},
		From:   guq.sql,
		Unique: true,
	}
	if unique := guq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := guq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, gooduser.FieldID)
		for i := range fields {
			if fields[i] != gooduser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := guq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := guq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := guq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := guq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (guq *GoodUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(guq.driver.Dialect())
	t1 := builder.Table(gooduser.Table)
	columns := guq.fields
	if len(columns) == 0 {
		columns = gooduser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if guq.sql != nil {
		selector = guq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if guq.unique != nil && *guq.unique {
		selector.Distinct()
	}
	for _, m := range guq.modifiers {
		m(selector)
	}
	for _, p := range guq.predicates {
		p(selector)
	}
	for _, p := range guq.order {
		p(selector)
	}
	if offset := guq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := guq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (guq *GoodUserQuery) ForUpdate(opts ...sql.LockOption) *GoodUserQuery {
	if guq.driver.Dialect() == dialect.Postgres {
		guq.Unique(false)
	}
	guq.modifiers = append(guq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return guq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (guq *GoodUserQuery) ForShare(opts ...sql.LockOption) *GoodUserQuery {
	if guq.driver.Dialect() == dialect.Postgres {
		guq.Unique(false)
	}
	guq.modifiers = append(guq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return guq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (guq *GoodUserQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodUserSelect {
	guq.modifiers = append(guq.modifiers, modifiers...)
	return guq.Select()
}

// GoodUserGroupBy is the group-by builder for GoodUser entities.
type GoodUserGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gugb *GoodUserGroupBy) Aggregate(fns ...AggregateFunc) *GoodUserGroupBy {
	gugb.fns = append(gugb.fns, fns...)
	return gugb
}

// Scan applies the group-by query and scans the result into the given value.
func (gugb *GoodUserGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := gugb.path(ctx)
	if err != nil {
		return err
	}
	gugb.sql = query
	return gugb.sqlScan(ctx, v)
}

func (gugb *GoodUserGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range gugb.fields {
		if !gooduser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := gugb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gugb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (gugb *GoodUserGroupBy) sqlQuery() *sql.Selector {
	selector := gugb.sql.Select()
	aggregation := make([]string, 0, len(gugb.fns))
	for _, fn := range gugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(gugb.fields)+len(gugb.fns))
		for _, f := range gugb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(gugb.fields...)...)
}

// GoodUserSelect is the builder for selecting fields of GoodUser entities.
type GoodUserSelect struct {
	*GoodUserQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (gus *GoodUserSelect) Scan(ctx context.Context, v interface{}) error {
	if err := gus.prepareQuery(ctx); err != nil {
		return err
	}
	gus.sql = gus.GoodUserQuery.sqlQuery(ctx)
	return gus.sqlScan(ctx, v)
}

func (gus *GoodUserSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := gus.sql.Query()
	if err := gus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gus *GoodUserSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodUserSelect {
	gus.modifiers = append(gus.modifiers, modifiers...)
	return gus
}
