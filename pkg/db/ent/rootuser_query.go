// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/miningpool-middleware/pkg/db/ent/rootuser"
)

// RootUserQuery is the builder for querying RootUser entities.
type RootUserQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.RootUser
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RootUserQuery builder.
func (ruq *RootUserQuery) Where(ps ...predicate.RootUser) *RootUserQuery {
	ruq.predicates = append(ruq.predicates, ps...)
	return ruq
}

// Limit adds a limit step to the query.
func (ruq *RootUserQuery) Limit(limit int) *RootUserQuery {
	ruq.limit = &limit
	return ruq
}

// Offset adds an offset step to the query.
func (ruq *RootUserQuery) Offset(offset int) *RootUserQuery {
	ruq.offset = &offset
	return ruq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ruq *RootUserQuery) Unique(unique bool) *RootUserQuery {
	ruq.unique = &unique
	return ruq
}

// Order adds an order step to the query.
func (ruq *RootUserQuery) Order(o ...OrderFunc) *RootUserQuery {
	ruq.order = append(ruq.order, o...)
	return ruq
}

// First returns the first RootUser entity from the query.
// Returns a *NotFoundError when no RootUser was found.
func (ruq *RootUserQuery) First(ctx context.Context) (*RootUser, error) {
	nodes, err := ruq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rootuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ruq *RootUserQuery) FirstX(ctx context.Context) *RootUser {
	node, err := ruq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RootUser ID from the query.
// Returns a *NotFoundError when no RootUser ID was found.
func (ruq *RootUserQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ruq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rootuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ruq *RootUserQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := ruq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RootUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RootUser entity is found.
// Returns a *NotFoundError when no RootUser entities are found.
func (ruq *RootUserQuery) Only(ctx context.Context) (*RootUser, error) {
	nodes, err := ruq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rootuser.Label}
	default:
		return nil, &NotSingularError{rootuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ruq *RootUserQuery) OnlyX(ctx context.Context) *RootUser {
	node, err := ruq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RootUser ID in the query.
// Returns a *NotSingularError when more than one RootUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (ruq *RootUserQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ruq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rootuser.Label}
	default:
		err = &NotSingularError{rootuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ruq *RootUserQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := ruq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RootUsers.
func (ruq *RootUserQuery) All(ctx context.Context) ([]*RootUser, error) {
	if err := ruq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ruq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ruq *RootUserQuery) AllX(ctx context.Context) []*RootUser {
	nodes, err := ruq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RootUser IDs.
func (ruq *RootUserQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := ruq.Select(rootuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ruq *RootUserQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := ruq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ruq *RootUserQuery) Count(ctx context.Context) (int, error) {
	if err := ruq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ruq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ruq *RootUserQuery) CountX(ctx context.Context) int {
	count, err := ruq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ruq *RootUserQuery) Exist(ctx context.Context) (bool, error) {
	if err := ruq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ruq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ruq *RootUserQuery) ExistX(ctx context.Context) bool {
	exist, err := ruq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RootUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ruq *RootUserQuery) Clone() *RootUserQuery {
	if ruq == nil {
		return nil
	}
	return &RootUserQuery{
		config:     ruq.config,
		limit:      ruq.limit,
		offset:     ruq.offset,
		order:      append([]OrderFunc{}, ruq.order...),
		predicates: append([]predicate.RootUser{}, ruq.predicates...),
		// clone intermediate query.
		sql:    ruq.sql.Clone(),
		path:   ruq.path,
		unique: ruq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RootUser.Query().
//		GroupBy(rootuser.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ruq *RootUserQuery) GroupBy(field string, fields ...string) *RootUserGroupBy {
	grbuild := &RootUserGroupBy{config: ruq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ruq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ruq.sqlQuery(ctx), nil
	}
	grbuild.label = rootuser.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.RootUser.Query().
//		Select(rootuser.FieldCreatedAt).
//		Scan(ctx, &v)
func (ruq *RootUserQuery) Select(fields ...string) *RootUserSelect {
	ruq.fields = append(ruq.fields, fields...)
	selbuild := &RootUserSelect{RootUserQuery: ruq}
	selbuild.label = rootuser.Label
	selbuild.flds, selbuild.scan = &ruq.fields, selbuild.Scan
	return selbuild
}

func (ruq *RootUserQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ruq.fields {
		if !rootuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ruq.path != nil {
		prev, err := ruq.path(ctx)
		if err != nil {
			return err
		}
		ruq.sql = prev
	}
	if rootuser.Policy == nil {
		return errors.New("ent: uninitialized rootuser.Policy (forgotten import ent/runtime?)")
	}
	if err := rootuser.Policy.EvalQuery(ctx, ruq); err != nil {
		return err
	}
	return nil
}

func (ruq *RootUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RootUser, error) {
	var (
		nodes = []*RootUser{}
		_spec = ruq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*RootUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &RootUser{config: ruq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ruq.modifiers) > 0 {
		_spec.Modifiers = ruq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ruq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ruq *RootUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ruq.querySpec()
	if len(ruq.modifiers) > 0 {
		_spec.Modifiers = ruq.modifiers
	}
	_spec.Node.Columns = ruq.fields
	if len(ruq.fields) > 0 {
		_spec.Unique = ruq.unique != nil && *ruq.unique
	}
	return sqlgraph.CountNodes(ctx, ruq.driver, _spec)
}

func (ruq *RootUserQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ruq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ruq *RootUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   rootuser.Table,
			Columns: rootuser.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: rootuser.FieldID,
			},
		},
		From:   ruq.sql,
		Unique: true,
	}
	if unique := ruq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ruq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rootuser.FieldID)
		for i := range fields {
			if fields[i] != rootuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ruq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ruq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ruq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ruq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ruq *RootUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ruq.driver.Dialect())
	t1 := builder.Table(rootuser.Table)
	columns := ruq.fields
	if len(columns) == 0 {
		columns = rootuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ruq.sql != nil {
		selector = ruq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ruq.unique != nil && *ruq.unique {
		selector.Distinct()
	}
	for _, m := range ruq.modifiers {
		m(selector)
	}
	for _, p := range ruq.predicates {
		p(selector)
	}
	for _, p := range ruq.order {
		p(selector)
	}
	if offset := ruq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ruq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ruq *RootUserQuery) ForUpdate(opts ...sql.LockOption) *RootUserQuery {
	if ruq.driver.Dialect() == dialect.Postgres {
		ruq.Unique(false)
	}
	ruq.modifiers = append(ruq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ruq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ruq *RootUserQuery) ForShare(opts ...sql.LockOption) *RootUserQuery {
	if ruq.driver.Dialect() == dialect.Postgres {
		ruq.Unique(false)
	}
	ruq.modifiers = append(ruq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ruq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ruq *RootUserQuery) Modify(modifiers ...func(s *sql.Selector)) *RootUserSelect {
	ruq.modifiers = append(ruq.modifiers, modifiers...)
	return ruq.Select()
}

// RootUserGroupBy is the group-by builder for RootUser entities.
type RootUserGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rugb *RootUserGroupBy) Aggregate(fns ...AggregateFunc) *RootUserGroupBy {
	rugb.fns = append(rugb.fns, fns...)
	return rugb
}

// Scan applies the group-by query and scans the result into the given value.
func (rugb *RootUserGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := rugb.path(ctx)
	if err != nil {
		return err
	}
	rugb.sql = query
	return rugb.sqlScan(ctx, v)
}

func (rugb *RootUserGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range rugb.fields {
		if !rootuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := rugb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rugb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (rugb *RootUserGroupBy) sqlQuery() *sql.Selector {
	selector := rugb.sql.Select()
	aggregation := make([]string, 0, len(rugb.fns))
	for _, fn := range rugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(rugb.fields)+len(rugb.fns))
		for _, f := range rugb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(rugb.fields...)...)
}

// RootUserSelect is the builder for selecting fields of RootUser entities.
type RootUserSelect struct {
	*RootUserQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (rus *RootUserSelect) Scan(ctx context.Context, v interface{}) error {
	if err := rus.prepareQuery(ctx); err != nil {
		return err
	}
	rus.sql = rus.RootUserQuery.sqlQuery(ctx)
	return rus.sqlScan(ctx, v)
}

func (rus *RootUserSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := rus.sql.Query()
	if err := rus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rus *RootUserSelect) Modify(modifiers ...func(s *sql.Selector)) *RootUserSelect {
	rus.modifiers = append(rus.modifiers, modifiers...)
	return rus
}
